@inherits UmbracoViewPage
@{
    Layout = null;
}

@{
    IEnumerable<IPublishedContent> rootNode = Umbraco.TypedContentAtRoot();
    var home = rootNode.First(x => x.IsDocumentType(Homepage.ModelTypeAlias));
	
	Response.Redirect(home.Url);
}